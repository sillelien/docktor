docktor:
  image: vizzbuzz/docktor
  links:
    - test
  roles:
    - global

test:
  image: alpine
  command: sleep 300
  environment:
    CHECK_COMMAND: "ping -c 1 ${TEST_ENV_TUTUM_SERVICE_FQDN}"
    FIX_COMMAND: "/scripts/fixes/tutum_restart.sh ${TEST_TUTUM_API_URL}"
